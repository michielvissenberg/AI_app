Database Link: We can now add a database/mongodb_client.py to push the validated FinancialStatement directly to your Atlas cluster.


done: Stabilize foundation and runtime integrity in requirements.txt, mapper.py, and main.py by resolving dependency/import mismatches and defining one explicit orchestrated flow (extract -> normalize -> map -> validate -> export).

done: Introduce a normalized intermediate representation for extracted table cells/rows in schemas.py and adapt pdf_parser/processors/dockling_engine.py + azure_engine.py to emit that shape so engines are interchangeable.

done: Harden value parsing and missing-data semantics in cleaners.py: separate 0, missing, and parse-failure states; preserve sign conventions (parentheses negatives), dashes, and scaled units.

done: Upgrade statement mapping logic in mapper.py from simple label slugging to rule-based classification for core 10-K statement types and period columns (current/prior).

done: Add dual outputs in main.py: canonical machine JSON (for reliability) and compact markdown narrative (for LLM context windows), with consistent section headers and metric/value/period formatting.

to do: Add structured logging + error taxonomy across processors (pdf_parser/processors/dockling_engine.py, azure_engine.py, main.py) so failures are diagnosable and recoverable.

to do: Build a small evaluation harness using sample filings from data plus expected outputs, and define quality KPIs (statement detection accuracy, line-item mapping accuracy, numeric parse accuracy).

to do: Capture next-phase architecture in next_steps.txt: provenance fields, hybrid fallback strategy, and persistence layer once MVP accuracy stabilizes.
